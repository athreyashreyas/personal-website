# Manual ordering overrides, edited in the CMS under "Ordering".
#
# Each list holds entry slugs, in the order they should appear at the top of
# their page. Anything not listed here falls in underneath, newest first — so
# an empty list means "pure date-added order", which is the default.
writing: []
recommendations: []
